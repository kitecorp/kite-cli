# Kite Project Configuration
# Project: {{projectName}}

version: "1.0"

# Provider configuration
providers:
  - name: files
    version: "0.1.0"

# State backend configuration
state:
  backend: local
  # For remote state:
  # backend: postgresql
  # connection: "postgresql://localhost:5432/kite_state"

# Default environment
default_environment: dev
